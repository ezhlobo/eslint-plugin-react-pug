/**
 * @fileoverview Add supporting of pugjs with react
 * @author
 */
"use strict";

const allRules = {
}

module.exports = {
  rules: allRules,
  configs: {
    all: {
      globals: {
        pug: true,
      },
      rules: {},
    },
  },
}
